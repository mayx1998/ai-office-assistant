llm:
  provider: dashscope
  model: qwen-plus
  base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  api_key_env: DASHSCOPE_API_KEY        # key 只从环境变量读，永不写进这个文件

mcp_servers:
  - name: office
    command: officecli
    args: ["mcp"]
  - name: contact
    command: python
    args: ["mcp_servers.py"]            # ← 按你通讯录 server 的真实文件名改

limits:
  max_steps: 10
  max_retries: 3
  mcp_timeout_s: 30
  cli_timeout_s: 30
  per_tool_max_calls: 15
  concurrency: 4          # 无状态工具的并发上限
  office_concurrency: 1   # Office 工具必须串行（真实 Office 是单实例）

paths:
  allowed_dirs:           # 第 4 步路径白名单用
    - D:\dev\基于MCP的AI办公助手\eval\sandbox
    - D:\dev\基于MCP的AI办公助手\outputs

trace:
  db: traces.db